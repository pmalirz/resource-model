pluginManagement {
    plugins {
        id("pl.malirz.intest") version "0.0.1-SNAPSHOT"
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

rootProject.name = "resource-model"
